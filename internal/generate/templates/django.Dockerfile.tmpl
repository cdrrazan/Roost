# Generated by roost for {{.Name}} — do not edit.
FROM python:{{.PythonTag}} AS builder
WORKDIR /app
COPY . .
RUN pip install --no-cache-dir --prefix=/install gunicorn \
	&& if [ -f requirements.txt ]; then pip install --no-cache-dir --prefix=/install -r requirements.txt; else pip install --no-cache-dir --prefix=/install .; fi

FROM python:{{.PythonTag}}
WORKDIR /app
RUN useradd --create-home app
COPY --from=builder /install /usr/local
COPY --from=builder /app /app
USER app
EXPOSE {{.Port}}
CMD {{.StartCommand}}
